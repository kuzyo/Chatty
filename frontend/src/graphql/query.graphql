query User {
  currentUser {
    _id
    name
    googleId
    email
    image
  }
}

query getUsers {
  getUsers {
    _id
    name
    googleId
    image
  }
}

query getMessages {
  getMessages {
    _id
    from
    to
    createdAt
    body
  }
}
